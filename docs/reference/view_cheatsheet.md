# View Package Cheat Sheet

Opens the manureshed package cheat sheet in your default PDF viewer. The
cheat sheet provides a quick reference for common functions and
workflows.

Opens the manureshed package cheat sheet in your default PDF viewer. The
cheat sheet provides a quick reference for common functions and
workflows.

## Usage

``` r
view_cheatsheet()

view_cheatsheet()
```

## Value

Opens PDF in browser, returns path invisibly

Opens PDF in browser, returns path invisibly

## Examples

``` r
if (FALSE) { # \dontrun{
# View the cheat sheet
view_cheatsheet()
} # }
if (FALSE) { # \dontrun{
# View the cheat sheet
view_cheatsheet()
} # }
```
